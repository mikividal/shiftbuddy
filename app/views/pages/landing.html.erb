<!DOCTYPE html>
<html>
  <head>
    <title>ShiftBuddy - Radiología</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
  </head>

  <body class="bg-light d-flex align-items-center min-vh-100">
    <div class="container">
      <div class="row justify-content-center">
        <div class="col-md-6 col-lg-5 col-xl-4">
          <div class="card shadow-lg border-0 rounded-4">
            <div class="card-body p-5 text-center">

              <!-- Logo -->
              <div class="mb-4">
                <div class="logo-container mb-3">
                  <i class="fas fa-heartbeat text-primary" style="font-size: 3rem;"></i>
                </div>
                <h1 class="fw-bold text-primary mb-0" style="font-size: 2.5rem;">ShiftBuddy</h1>
              </div>

              <!-- Description -->
              <div class="mb-4">
                <p class="text-muted fs-6 lh-base">
                  Task management platform designed specifically for radiology residents working on-call shifts.
                  Never miss a STAT case again.
                </p>
              </div>

              <!-- Auth Buttons -->
              <div class="d-grid gap-3">
                <%= link_to new_user_session_path, class: "btn btn-primary btn-lg py-3" do %>
                  <i class="fas fa-sign-in-alt me-2"></i>
                  Log In
                <% end %>

                <%= link_to new_user_registration_path, class: "btn btn-outline-primary btn-lg py-3" do %>
                  <i class="fas fa-user-plus me-2"></i>
                  Sign Up
                <% end %>
              </div>

              <!-- Footer note -->
              <div class="mt-4">
                <small class="text-muted">
                  Built by radiologists, for radiologists
                </small>
              </div>

            </div>
          </div>
        </div>
      </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>

    <style>
      body {
        font-family: 'Inter', sans-serif;
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      }

      .card {
        backdrop-filter: blur(10px);
        background: rgba(255, 255, 255, 0.95);
      }

      .rounded-4 {
        border-radius: 1rem !important;
      }

      .btn {
        font-weight: 500;
        border-radius: 0.5rem;
      }

      .logo-container i {
        animation: pulse 2s infinite;
      }

      @keyframes pulse {
        0% {
          transform: scale(1);
        }
        50% {
          transform: scale(1.05);
        }
        100% {
          transform: scale(1);
        }
      }
    </style>
  </body>
</html>
